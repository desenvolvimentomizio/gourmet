program MizioImpressoras;

uses
  Vcl.Forms,
  ufPrinciMizioImpressoras in 'ufPrinciMizioImpressoras.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
